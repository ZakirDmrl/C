#include <assert.h>
#include <stdio.h>
#include <complex.h>
#include <locale.h>

int main(){
// int value = 5;
// assert(value != 0);
// printf("Value= %d",value);


// double complex z = 3.0 + 4.0 * I;
// double zcabs = cabs(z);
// printf("z = %.0f + %.0fi\n",creal(z),cimag(z));
// printf("zcabs = %.0f",zcabs);
// double complex zccos =ccos(z);
// double complex zcsin =csin(z);

// printf("zcocos = %f + %fi\n",creal(zccos),cimag(zccos));
// printf("zcosin = %f + %fi",creal(zcsin),cimag(zcsin));

/* Yerel verileri Türkçe'ye çevirme*/
//struct lconv *lc;

// printf("%s\n", setlocale(LC_ALL, "Turkish"));
// lc = localeconv();
// printf("Ondalık işareti: %s\n", lc->decimal_point);
// printf("Binler basamağı işareti: %s\n", lc->thousands_sep);
// printf("Uluslararası para sembolü: %s\n", lc->int_curr_symbol);

// /*Yerel verileri İspanyolcaya çevirme*/
//     printf("\n%s\n", setlocale(LC_ALL, "Spanish"));
//     lc = localeconv();
//     printf("Ondalık işareti: %s\n", lc->decimal_point);
//     printf("Binler basamağı işareti: %s\n", lc->thousands_sep);
//     printf("Uluslararası para sembolü: %s\n", lc->int_curr_symbol);

/* Yere verileri Japoncaya çevirme*/
    // printf("\n%s\n", setlocale(LC_ALL, "Japanese"));
    // lc = localeconv();
    // printf("Ondalık işareti: %s\n", lc->decimal_point);
    // printf("Binler basamağı işareti: %s\n", lc->thousands_sep);
    // printf("Uluslararası para sembolü: %s\n", lc->int_curr_symbol);
    // printf("Grouping %i\n",lc->grouping);
    // printf("frac_digits: %s",lc->frac_digits);
    
return 0;
}